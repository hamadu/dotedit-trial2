import { reduce } from "../../src/js/reducers/Reducer";

describe("Reducer", () => {
  beforeEach(() => {
  });

  it("testing test", () => {
      const result = 1+2;
      chai.assert.strictEqual(result, 3);
  });
});
